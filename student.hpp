#pragma once // prevents multiple definitions

#include <iostream> // print library
#include "./AbstractDataType/calculator.h" // reference implementations

// Exercise 1: implements function 'add', that adds two numbers in the Caclulator class

// Exercise 2: implements function 'mul', that multiplies two numbers in the Caclulator class

