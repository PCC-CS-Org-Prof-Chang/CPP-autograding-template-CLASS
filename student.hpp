#pragma once // prevents multiple definitions

#include <iostream> // print library
#include "./AbstractDataType/calculator.h" // reference implementations

// Exercise 1: implements member function 'add', that adds two numbers in the Caclulator class in calculator.cpp

// Exercise 2: implements member function 'mul', that multiplies two numbers in the Caclulator class in calculator.cpp

